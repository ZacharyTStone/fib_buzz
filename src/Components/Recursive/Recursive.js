import "./Recursive.css";

const Recursive = () => {
  return (
    <div className="Recursive">
      <h1>Recursion</h1>
      <p>I solved this problem using an iterative approach.</p>
      <p>Here is how I would solive it recursively:</p>
    </div>
  );
};

export default Recursive;
